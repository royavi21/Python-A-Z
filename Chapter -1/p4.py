import os
# specify the directory path
directory_path = 'C:/Users/user/Desktop/cgpa' 
# list the files and directories in the specified path
contents = os.listdir(directory_path)
# print the file and directory name
for item in contents:
    print(item)

# p5 is to lable with comments; which I just did here.
#  [use comments in the code in the right place]