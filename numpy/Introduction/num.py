import numpy as np

temprature = np.array([32.4 , 23.2, 35.5, 43.7, 22.3])

avg = np.mean(temprature)
print(f" Avarage using np is: {avg:.2f}")

#Here is the example with numpy - which is fast and efficient